spring:
  datasource:
    url: jdbc:h2:mem:idempotency-db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  logging:
    level:
      root: INFO
      org.springframework.jdbc.core: DEBUG